#!/bin/sh

#close all eww bars
if [ "$(pgrep -x eww)" ];then
	eww -c ~/.config/eww/right-bar kill
else
	sh ~/.config/eww/right-bar/launch_bar
fi

awesome-client '
            for s in screen do
                s.mywibox.visible = not s.mywibox.visible
                if s.mybottomwibox then
                    s.mybottomwibox.visible = not s.mybottomwibox.visible
                end
            end
			'

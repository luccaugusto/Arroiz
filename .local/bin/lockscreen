#!/usr/bin/env sh

#-Locks the screen, pauses songs and videos.

playerctl -a pause &>/dev/null

if [ "$(pgrep -x Hyprland)" ]; then
	hyprlock
elif [ "$(pgrep -x Xorg)" ]; then
	# i3customlock
	xscreensaver-command -lock
fi
